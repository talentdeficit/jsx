language: erlang
install: dialyzer --build_plt --apps erts kernel stdlib --output_plt .plt
script: rebar compile && rebar skip_deps=true eunit && dialyzer --plt .plt ./ebin
otp_release:
  - R15B02
  - R16B03-1
  - 17.5
  - 18.3
  - 19.2

